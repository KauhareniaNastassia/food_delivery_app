name: main_page_view
description: Main page view feature
version: 0.0.1
publish_to: none

environment:
  sdk: '>=3.0.0 <4.0.0'
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter
  core:
    path: ../../core
  domain:
    path: ../../domain
  data:
    path: ../../data
  core_ui:
    path: ../../core_ui
  navigation:
    path: ../../navigation
  shopping_cart:
    path: ../shopping_cart
  settings:
    path: ../settings
  auth:
    path: ../auth
  admin_panel:
    path: ../admin_panel

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0
